import { React } from "react";
import { Searchbar } from "react-native-paper";
import { StatusBar, StyleSheet, SafeAreaView, Text, View } from "react-native";

export const Resaturant = () => (
    <Text>RestaurantsInfo</Text>
)
